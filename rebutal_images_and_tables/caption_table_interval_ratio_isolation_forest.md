**Outlier-to-inlier interval length ratio under Isolation Forest (higher is better):** ILR results using QNN as the shared backbone across all methods. Outliers are defined via Isolation Forest with $5\%$ contamination; inliers are the $20\%$ most inlying points according to the same criterion. Parentheses show $2\times$ the standard deviation; bold marks the best method within a $95\%$ confidence interval (ties allowed). CREDO is the top performer across all 8 datasets, with competing methods displaying notably degraded performance relative to the LOF-based evaluation — confirming that CREDO's epistemic adaptivity is robust to the choice of outlier definition.