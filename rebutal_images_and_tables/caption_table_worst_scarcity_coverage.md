**Worst Scarcity Quantile Coverage (target $1 - \alpha = 0.90$):** Worst-case coverage across scarcity quantile bins, using QNN as the shared backbone across all methods. Observations are ranked by a kNN-based scarcity score and split into 4 bins, from the most populated to the most data-sparse regions. Parentheses show $2\times$ the standard deviation; bold marks the highest coverage within a 95\% confidence interval. While methods perform similarly on most datasets, CREDO consistently ranks among the top performers across all datasets, whereas competing methods fail to do so in at least one dataset.